// Generated by CoffeeScript 1.6.1
(function() {

  describe('String contains', function() {
    it('returns true when a string contains a substring', function() {
      var s, t;
      s = 'abra';
      t = 'kadabra';
      return expect(t.contains(s)).toEqual(true);
    });
    it('returns false when a string doesnt contain a substring', function() {
      var s, t;
      s = 'abra';
      t = 'xyz';
      return expect(t.contains(s)).toEqual(false);
    });
    it('returns false if the lead string is empty', function() {
      var s, t;
      s = '';
      t = 'abra';
      return expect(s.contains(t)).toEqual(false);
    });
    return it('returns true if the substring is ever empty', function() {
      var s, t;
      s = '';
      t = 'abra';
      expect(t.contains(s)).toEqual(true);
      t = '';
      return expect(t.contains(s)).toEqual(true);
    });
  });

}).call(this);
